server:
  port: 8080
# ×¢²áÖĞĞÄÅäÖÃ
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka/
spring:
  application:
        name: @artifactId@